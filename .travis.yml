language: python

services:
  - docker

before_install:
  - docker build -t tanukiraccoon/EN-to-TH-Transliteration .
  - docker run -d -p 127.0.0.1:80:4567 tanukiraccoon/EN-to-TH-Transliteration /bin/sh -c "cd /root/EN-to-TH-Transliterationsinatra; bundle exec foreman start;"
  - docker ps -a
  - docker run tanukiraccoon/EN-to-TH-Transliteration /bin/sh -c "cd /root/EN-to-TH-Transliteration; bundle exec rake test"

script:
  - bundle exec rake test